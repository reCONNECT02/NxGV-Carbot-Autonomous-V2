# Backlog: things to change or add

Read at the start of every phase chat; updated at the end of it.
Status: TODO / DOING / DONE (phase) / WAITING (on the team).

| # | Request | Phase | Status | Notes |
|---|---|---|---|---|
| 1 | Stack reads track_map.yaml + mission.yaml **version 2** (from tools/map), still reads version 1 | 4 | DONE (4) | v2 is the default; v1 kept in `config/data/v4_reference/` |
| 2 | Leg 3: parallel bay -> perpendicular bay (Challenge 11) | 4 road part, 5 bay exit/entry | DONE (5) | Un-park = time-reversed parking search; park perpendicular via docking straight. Sim: both PARKED |
| 3 | Boom gate open/closed model (front camera) | 6 | WAITING | Team is training its own model; plug it in when supplied. Mission logic reads `DetectionArray.boom_gate_state` (+ confidence) only |
| 4 | Boom gate positions on the track (challenge4 + roundabout gate) | 4/8 | WAITING | `track_features.yaml boom_gates.*` (provisional, V4/rulebook values). Measure on site |
| 5 | Show all speed zones on the map tab | 7 | TODO | Zones: `mission_rules.yaml speed_zones`; active zone in `MissionState.speed_zone` |
| 6 | Change the speed of each zone from the GUI | 7 | TODO | Tuning tab edits `mission_rules.yaml speed_zones[].max_speed_mps`; calibrate mode only (race = read-only) |
| 7 | Corridor.msg frame comment: odom -> track | 4 | DONE (4) | |
| 8 | Confirm P1 (x 6.75, y 2.47, facing south) is the traffic-light stop line | 4 | WAITING | `track_features.yaml light_goal_pose` + `traffic_light` position derived from it (provisional) |
| 9 | Map lap must also drive the parking road (parking_spur, parking_corner, perp_row not fitted) | map / 8 | WAITING | `track_map.yaml fit_report`; then re-run `mission_planner.py` |
| 10 | Measure speed bump, hill, tunnel positions | on site | WAITING | `track_features.yaml` (provisional V4 values) |
| 11 | After ANY map edit, re-run `mission_planner.py` | always | note | Otherwise block 07 fails: "planned on a different track_map.yaml" |
| 12 | Parking previews are at the minimum turning radius: block 11 must replan from the actual pose | 5 | DONE (5) | Plans from the pose, cusp + heading replans, `plan_radius_factor 1.08` |
| 13 | Run calibration steps 7/8 on the car (feedforward / steering are placeholders) | 8 / on site | TODO | `calib_steering`, `calib_speed`; also confirm `vehicle.wheelbase_m` |
| 14 | Measure planner / parking CPU on the RDK X5 | on site | TODO | Laptop: parking 0.04-0.5 s, recovery search ~0.3 s |
| 15 | Collect boom gate footage (open + closed, several distances) from our Astra, validate, then set `challenge4_gate.enabled: true` | 6 / on site | TODO | Team model had 0 boom validation images. `docs/DETECTORS.md` |
| 16 | Validate the light on the car, then set `traffic_light.enabled: true` | 6 / on site | TODO | While false, challenge 7 = 0 but the run never waits. Check `object_size_m` and `gate_association` tolerances too |
| 17 | `run_planning.py --v4` crashes: v1 manoeuvre piece has no points, so `ParkingSession.start` has no goal | 5 | TODO | Found in phase 6. Team mission, race and tests unaffected. Fix: goal from the v1 bay, or `parking='preview'` for `--v4` |
| 18 | Run calibration step 1 on the car; check system_monitor CPU with 3 raw camera subscriptions | 8 / on site | TODO | `top`: system_monitor should be well under 10 % of a core. If high, lower watch set or add a sampled mode |
| 19 | `bpu_ratio_path` (BPU load) still unverified | 8 / on site | TODO | system_monitor logs one warning and reports -1 if the file is missing |
| 20 | Make step 12 `mission_planner` required once its page exists | 8 | TODO | calibration_steps.yaml `required: false` for now |
| 21 | KEEP_PREVIOUS for data-writing steps must copy their data files | 8 | DOING | Hook `StepImpl.keep_data` exists; step 2 implements it (refuses when the old confirmation misses a camera enabled now), step 6 too (copies the 3 servo_controller overlay values, refuses if one is missing, sets them live).; step 5 too (cameras.yaml lidar_to_camera + both overlay keys). Each later data-writing page must too |
| 22 | mipi_cam auto-detects sensors and ignores `channel`: with one MIPI sensor missing, the other one is published under the wrong namespace | 8 | WONTFIX (MIPI cameras removed 2026-09-24) | Seen 2026-09-23 on risabot5: IMX219 undetected, the `/cam_imx219` (ch 0) process opened the OV5647 (i2c4@0x36, host 2), the `/cam_ov5647` process then failed with "detected sensors are 1 less than expected". Step 1 should read the "cap <sensor> init success" line (/tmp/carbot_root_logs) or `dmesg` and fail the row on a sensor/namespace mismatch |
| 23 | Step 1 on the car: CPU load ~16 with calibrate.launch.py | 8 / on site | TODO | Seen 2026-09-23 on risabot5: gui_server ~60 %, system_monitor ~50 %, most planning/control nodes 35-45 % of a core each (old risabot5-track-stack.service was also running; now disabled). Re-measure alone, see #18 |
| 24 | Side MIPI cameras (OV5647, IMX219) REMOVED from the whole project (was: switched OFF, stack runs front-only) | 8 | DONE (2026-09-24) | 2026-09-23 risabot5: IMX219 not detected, OV5647 MIPI frame errors (also alone, as in Camera_Setup.md), cable reseat did not help. Launch, camera restart, steps 1-4 and road_perception skip disabled sensors. Still to honour it: race preflight (use `data.unconfirmed_roles`). Re-enable: fix hardware, set `enabled: true`, rebuild |
| 25 | Run calibration step 2 on the car (front-only): wave at the Astra, Confirm, Save; check `<session>/data/cameras.yaml` | 8 / on site | TODO | Needs camera_preview publishing `/carbot/cam/front/preview/compressed`. The preview tile shows what the launch loaded; saved roles apply from the next launch of that session |
| 26 | Run calibration step 3 on the car (front only): Run on the Astra, cover the 3x3 grid, Save; check `<session>/intrinsics/astra.yaml` and `data/cameras.yaml intrinsics_file` | 8 / on site | TODO | Untested on hardware. Watch CPU while the wizard decodes 640x480 frames (detection at tick_hz 5); if slow, lower tick_hz or add a frame stride |
| 27 | Run calibration step 5 on the car: target at 3+ spots, Capture, Compute, Save, relaunch the session, check `tf2_echo base_link laser_frame` and the tunnel follower | 8 / on site | TODO | Needs steps 3/4 passed (front intrinsics + mount) and the LiDAR publishing (see step 1). Target: bottle/can/pole 3-12 cm wide, taller than the LiDAR |
| 28 | Measure the LiDAR mount: drivers.yaml `carbot_tf.base_to_laser` has x 0.0 / z 0.12, the V4 model says x 0.09 / z 0.22 | 8 / on site | TODO | Type the tape values into step 5 (x from the rear axle, height above the floor). A large "distance differ" warning in step 5 points to a wrong x |
| 29 | `risabot_automode/boom_gate_detector` declares its own `lidar_angle_offset` default 1.5708 (not pi) | 8 | TODO | Not launched by carbot_bringup today. If it is ever re-enabled, give it the step 5 yaw too (overlay) |
| 30 | gui_server shows EVERY node stale (itself included) on risabot5, so calibration pages say "calibration_wizard is not running" although it is | 7/8 | TODO | Seen 2026-09-23 10:35, load ~17: `/carbot/status` probe got every node at 1 Hz (max gap 1.01 s), `/api/tab/health` had all 20 nodes `stale`; service calls still worked. Find why gui_server's status callbacks lag > `stale_after_s` 3 s (rosout flood from tunnel_wall_follower at 10 Hz? executor starvation?). Workaround: the banner is cosmetic, actions still reach the wizard |
| 31 | Base servo_controller yaw with `imu_yaw_scale` != +-1: it scales THEN re-normalises, so `/imu/rpy` yaw jumps by 360 x (1 - abs(scale)) deg at +-180 (3 % -> ~11 deg step). local_pose uses the absolute yaw, so the heading jumps once per turn | 8 | TODO | Found building the step 6 page. The page measures the spin at scale +-1 to stay correct, and warns in the result when it saves a non-unit scale. Fix = in `servo_controller` normalise the offset difference, scale, and do NOT re-wrap (or unwrap in local_pose). Base-code change: owner to approve. Until then prefer a unit scale (pass within 3 % keeps +-1) |
| 32 | Run calibration step 6 on the car (IMU + wheel odometry page): 2 distance runs, 2 spins, 60 s drift, Save; check `<session>/params_overlay.yaml servo_controller` | 8 / on site | TODO | Car pushed by hand, motors off. Corrections are set live, so step 7 in the same launch uses them |
| 33 | `calib_odometry` (terminal) drift hint says `ros2 topic pub --once /imu/calibrate std_msgs/String` for a hardware IMU calibration; servo_controller only accepts JSON `{"action": "zero"}` / `"set_scale"`, so that command does nothing | 8 | TODO | Wizard page gives a power-cycle-still fix instead. Also `topics.py IMU_CALIBRATE` comment says "String (empty)" |
| 34 | `imu_yaw_scale` is not in `base_nodes.yaml` (servo_controller declares default 1.0) | 8 | TODO | Tunable should be in YAML. Adding the key = new key only (value 1.0); step 6 writes it into the session overlay anyway |
| 35 | Run calibration step 7 on the car (servo + steering page): Run, Go x 4+, Save; check `<session>/params_overlay.yaml` servo_center, command_owner steering, min_turning_radius_m | 8 / on site | TODO | The car drives ITSELF (duty 0.16): clear 1.5 x 2.5 m floor, hand on STOP MOTORS. Needs step 6 saved in the same session first. Watch the servo at full lock (buzzing -> lower servo_range_* in Tuning) |
| 36 | Merge wizard pages 8-13 to `main` | 8 | DOING | Integrated locally on `phase8/complete-calibration` with page 4 and page 5; package tests passed except two existing Windows-only failures. Robot validation and review remain before merge to `main` |
| 37 | Practice runs (step 13) can't drive: in calibrate mode nothing publishes `/carbot/race/armed`, so mission_logic stays IDLE | 8 | WAITING | Owner decision: how should practice be armed? Until then attempts record time + grade only |
| 38 | Probable bug: road_perception seed box `seed.x -0.25..0.45 m` is outside the front camera view (floor visible from x ~0.47 m) on the front-only car -> `grown` / `connected` always 0 | 2 / 8 | WAITING | Found by page 9. Owner decision: move to ~0.47..0.70? |
| 39 | Merge page 9's parameter access into `servo_link.ServoLink` | 8 | DONE | Integrated wizard uses `ServoLink` for road_perception; no duplicate ParamLink module |
| 40 | `test_import.py` exists in several packages, so they can't run in one pytest command | 8 | TODO | Run pytest one package at a time, or rename |
| 41 | Car tests for pages 8-13 on the RDK X5 | 8 / on site | TODO | Per-page list in `docs/PHASE8_PAGES_7-13_HANDOFF.md` "Untested on hardware". Page 8 drives itself: 1.5 m clear floor |
| 42 | Step 2 camera identity: images were ~1.2 s old (RDK under load), so `pass.max_image_age_s` 1.0 failed. Loosened to 2.0 to move on | 8 / on site | TODO | Seen 2026-09-24 on risabot5 with calibrate.launch.py. Root cause likely CPU load (#23). Reduce load / preview rate, then restore 1.0 in `calibration_steps.yaml`. Also gui_server: idle groups now stay subscribed (no destroy_subscription, InvalidHandle crash), so re-check its CPU |
| 43 | Astra publishes 320x240 on `/camera/color/image_raw`, but `cameras.yaml sensors.astra` says 640x480 | 8 / on site | TODO | Seen 2026-09-24 on risabot5: step 3 showed "no board in view" because the live detector halved every frame (160x120). `calib_core.detect_chessboard(fast=True)` now halves only frames >= 640 px wide. Decide which resolution is wanted (driver launch vs YAML), make them agree, then redo step 3 |
| 44 | Step 3 capture loosened for hand-held use: `extra_views` 10->3, `capture_timeout_s` 300->600, `novelty` 0.12->0.07, `still_px` 1.5->3.0; green overlay held 1 s after each capture | 8 / on site | TODO | 2026-09-24 on risabot5: owner could not collect views. Check the fit still meets `max_reprojection_px 0.8`; if views end up too similar (poor fit), raise `novelty` back toward 0.12 |
| 45 | Lighter calibration: `calibrate.launch.py calibrate_profile:=lite` (default `full` = unchanged) | 8 / on site | TODO (untested on car) | Starts only `drivers.yaml carbot_launch.calibrate_lite_keep` (road_perception, camera_preview, uwb_ranges, safety_monitor, command_owner, system_monitor, servo_controller) and loads `params/calibrate_lite.yaml` (lower GUI / road_perception / monitor rates). Drops planners, localisation, detector, tunnel nodes. Step 13 practice runs needs `full`. Measure `top` on the RDK, and check steps 1-12 still behave (esp. 6 IMU/odom, 7/8 driving, 9 venue thresholds, 10/11 UWB) |
| 46 | RDK X5 runs hot (SoC 88 C on risabot1 under calibrate) and slows down | on site | TODO | Add a fan / heatsink, keep the enclosure ventilated, close desktop apps on the RDK (`update-manager` used a full core) |
| 47 | Measured CPU on risabot1 (2026-09-24, `tools/measure_load.py`, car standing still): bare OS ~0 %; old `track_test` stack 76 % of the machine (SoC 76 C); Carbot **calibrate full** stack 97 % (load 21.5, SoC 83.5 C). Top: astra_camera_container 67, gui_server 65 (browser open), recovery_planner 60, bpu_detector 53, parking_planner 42, road_perception 41, system_monitor 39, local_planner 38, safety_monitor 37 (% of one core) | 8 / on site | TODO | Nodes with nothing to do (recovery/parking/local planner, corridor, path_tracker, local_pose, global_pose) burn 30-60 % each while stationary: find the timer rates / busy loops. Astra container: check depth/IR streams are off. Not measured yet: `race.launch.py` (launch was blocked by the permission classifier) |
| 48 | Astra colour only, 15 fps (`carbot_bringup/launch/astra_rgb.launch.py`, chosen by `cameras.yaml sensors.astra.launch_package/launch_file/launch_args`) | 8 / on site | DONE on risabot1 (2026-09-24) | astra_camera_container 67 % -> 12.6 % of a core; color 320x240 @ 15.0 Hz verified; only /camera/color/* topics remain (no depth, IR, D2C, point clouds, camera TF). Total CPU stayed ~98 % (the other nodes soak up what is free), see #47. Not yet on risabot5 or in an existing session (a session's own cameras.yaml copy keeps the old launch until re-saved). `cameras.yaml sensors.astra.encoding` says rgb8 but the driver publishes bgr8 (unchanged, check). If a camera swap needs the depth stream, use `launch_package: astra_camera`, `launch_file: astra_mini.launch.py`, `launch_args: []` |
| 49 | Where the idle CPU goes (py-spy + perf on risabot1, 2026-09-24): `perf` says 59 % of all CPU is the Python interpreter, only ~6 % FastDDS, 12 % kernel. py-spy: in recovery/parking/local_planner ~90 % of Python time is rclpy itself (wait-set rebuild per callback, thread-pool `submit` in MultiThreadedExecutor, `_take_subscription` deserialising messages), not planner logic. Hot inputs: `/odom` 20 Hz x 9 subscribers (Odometry -> Python objects), `/tf` 31 Hz, `/scan` 10 Hz x 6, `/imu/rpy` 20 Hz | 8 / on site | DOING | Ideas, biggest first: (a) drop parking_planner/recovery_planner MultiThreadedExecutor -> single-threaded (check long searches), (b) fewer /odom subscribers or 10 Hz for those that do not need 20, (c) only start planners while their challenge is active, (d) TF listeners only where needed. gui_server: `legs()` walked the whole route on every `/api/core` poll (~30 % of its time): now cached, `gui.yaml legs_refresh_s: 0.5`. Tool: py-spy (`pip install --user py-spy`) installed on risabot1 |
| 50 | Root cause of the idle CPU: every received message costs a bare rclpy node ~4.4 ms of CPU on the RDK (empty callback, `/tmp/bench.py` on risabot1: /odom 19 Hz = 9.8 % of a core, /scan 9.6 Hz = 4.2 %, /carbot/safety/status 41 Hz = 17.9 %). So CPU ~ (messages/s) x (subscribers). Per-topic load = rate x subscribers: **/carbot/safety/status 41 Hz x 6**, **/odom 20 Hz x 12**, /scan 10 Hz x 7, plus local_pose, road_grid, memory grid, corridor... ~800 msg/s in total = far more than 8 cores can decode, so every node lags and the machine sits at 98 % whatever one node saves | 8 / on site | TODO | Done so far (real but hidden by saturation): Astra colour only (#48), TF listeners -> `/tf_static` only in safety_monitor/local_planner/recovery_planner (`carbot_common/static_tf.py`, /tf now has 2 subscribers instead of 5), gui_server leg cache. Needs a TEAM decision (safety path, untested on the car): publish `/carbot/safety/status` at 10-20 Hz to non-owner subscribers or on change + heartbeat; fewer /odom subscribers (12) or 10 Hz for ones that only need freshness; planners subscribe to scan/odom only while their mode is active; single-threaded executors for parking/recovery; then re-measure below 100 % |
| 51 | `race.launch.py` measured on risabot1 (2026-09-24, idle, unarmed, no session, GUI open in 1-2 tabs, `tools/measure_load.py --seconds 120`): **97 % of the machine, load 18-26, SoC 88.4 C max, DDR 87.7 C**, CPU clock stays 1500 MHz. `/carbot/safety/status` arrives at 19.4 Hz, **max gap 90 ms vs command_owner.safety_max_age_s 0.1 s** (that is 10 ms from SAFETY STOP). Top: gui_server 64, recovery_planner 57, bpu_detector 56, system_monitor 52, parking_planner 47, local_planner 45, safety_monitor 43, command_owner 43 | 8 / on site | TODO | The race stack alone saturates the RDK, so calibrating under it (as intended) and racing on it both risk stale safety status. Done: safety status = change + 25 Hz heartbeat (`control.yaml safety_monitor.status_publish_hz`, `publish_due` in safety_core, tested). Still open: bpu_detector 56 % runs at 10 Hz though traffic-light/boom-gate triggers are disabled (#15/#16) - lower `detectors.yaml rate_hz` needs `debounce_frames` x period < `state_expiry_s` (3 x 0.2 s = 0.6 > 0.4); system_monitor 52 % (psutil process scans); /odom stays 12 subscribers (EvidenceInputs pairs local_pose with odom stamps, cannot decimate); single-threaded executors and mode-gated subscriptions not done (safety path, needs car test) |
| 52 | bpu_detector: only traffic light, boom gate, speed bump drive anything | 6 / on site | DONE on risabot1 (2026-09-24) | (a) `detector_core.decode_level` clears a lowest-threshold test on the raw logits first, then argmax/sigmoid only on the survivors (same output, tested against the old code; ~12 % faster on a laptop). (b) `detectors.yaml class_map`: the 8 INFO-only signs are `""` (ignored); old list kept as a comment. Measured on risabot1: per-frame `inference_ms` 96/61/41 -> 36/32/28, process CPU 54.9 -> 50.0 %. `rate_hz` 10 unchanged (debounce 3 x 0.1 s < `state_expiry_s` 0.4). SoC reached 91.9 C during this run: watch temperature (#46). speed_bump_sign is still `""` (untrained) |
| 53 | `carbot_planning/test/test_mission.py::test_closed_loop_full_mission` is load sensitive | 8 | TODO | 2026-09-24 (Windows laptop): failed (`MISSION COMPLETE` not reached) in a full ~50 min carbot_planning run made while the laptop was busy; the same single test passed in 59 s on untouched HEAD, then later timed out (>200 s) on untouched HEAD, and on our branch (>120 s). `sim_core` imports only `*_core` modules (not local_planner / ros_util, the two planning files we changed), and `route_core.py` has a wall-clock `time_budget_s` (`_time.monotonic()`), so a slow machine can truncate the search. Cause not proven. Re-run it alone on an idle machine / the RDK before trusting or blaming it |
| 54 | Step 4 front board only, rear axle 150 mm further back: `calibration_steps.yaml` step 4 `target.axle_offset_m: 0.150` (boards = [front], base_link x 0.77); `carbot_common.calib_tools.floor_board_dicts` applies it | 8 / on site | TODO (not tried on the car) | Sheet is now 470 x 1037 mm (24 in roll, too long for A1): reprint `docs/calibration/floor_sheet.pdf`. All 4 outer corners are in the front picture (2 of 4 at x 0.62), but the board is seen at a grazing angle: `calib_core.detect_chessboard` fails on synthetic renders at 320x240 always and at 640x480 flakily at x 0.70-0.75 (works to ~0.68), so `test_extrinsics_cli_offline` is xfail(strict). Decision 2026-09-24: capture at 640x480 (#55). Still open: make detect_chessboard more tolerant, or turn the board (yaw 0, layout centre_m x 0.50-0.53) if it still fails on the car. `calib_extrinsics.py` default `--roles` still lists left_rear/right_rear (goes away with the side-camera removal) |  **Update 2026-09-24:** real frame from risabot5 (640x480, board at x 0.77, rows 10-20 px apart, lens distortion visible) was NOT found by `detect_chessboard`; fixed with a vertical-stretch fallback (`calibration_steps.yaml extrinsics_ipm.target.detect_vertical_stretch: [2.0, 2.0, 2.0, 1.5, 2.5, 3.0]`, `calib_core.detect_chessboard(stretch=...)`). cv2.findChessboardCornersSB is not repeatable (19/20 at x2.0 on the same frame), hence the repeats. Real frame kept as test data (`carbot_perception/test/data/step4_front_640x480.png`); `test_extrinsics_cli_offline` no longer xfail. Untested end to end on the car (needs step 3 intrinsics at 640x480 first)  **Update 2026-09-24 (owner, on the car):** at `axle_offset_m: 0.150` the front board was NOT detected; the owner pushed the car forward so the rear axle is 65 mm behind the reference line: `axle_offset_m: 0.065` (front board base_link x 0.685; sheet regenerated: 470 x 952 mm, still a 24 in roll).
| 55 | Astra 640x480 @ 15 fps (`cameras.yaml sensors.astra` width/height/fps; was 320x240) for calibration AND driving | 8 / on site | DONE on risabot5 config, steps 3-4 TO REDO | Measured on risabot5, front-only stack: astra_camera_container 12 -> 24 %, road_perception 38.7 -> 52.5 %, bpu_detector 38.4 -> 41.6 %, load 19 -> 22-25, SoC 66.0 -> 66.3 C, stream 15.0 Hz. Cost ~ +26 points of one core on a saturated RDK (#47-#51). Resolution is detail, NOT field of view (Astra hfov ~60 deg): it does not widen the view in tight corners. Intrinsics and extrinsics belong to the size they were captured at: redo step 3 (and 4) at 640x480; old 320x240 sessions no longer match. Re-measure the race stack at both sizes after the CPU work is merged |
| 56 | Try `local_pose.heading_lead_s` 0.30-0.35 on the car (cancels servo_controller's IMU EMA lag; default 0 = V4) | 8 / on site | TODO (sandbox only) | `control_servo/servo_controller.py` runs an EMA `alpha = 0.15` on the IMU yaw (~0.28 s): in a turn `/imu/rpy` trails the real heading by yaw_rate x tau (25 deg/s -> ~7 deg). Sandbox (`tools/sandbox/run_icp_eval.py`): map-matched local error 15.3 -> 3.7 cm rms, sideways 8.7 -> 1.9 cm, heading 7.1 -> 2.1 deg at 0.35; robust to a 15 % `/odom` yaw-rate error. Verify on the car with a recorded run (`run_localization.py --bag`), then set it in `localization.yaml` / a calibration overlay. `docs/LOCALIZATION_MEMORY.md` |
| 57 | ICP against accumulated camera edges (`icp_core.py`, `local_pose.icp.*`) is built and tested offline but stays OFF | 8 | DONE as experiment (off) | Sandbox: 3-6 % better than map matching at best, neutral or worse once #54 is on or with noisy edges; cannot fix along-track (wheel scale) error; ~10-16 % of an RDK core while enabled (estimate). Enable only for an experiment: `icp.enabled: true`, watch `icp acc/att` in the local_pose status. Numbers and limits: `docs/LOCALIZATION_MEMORY.md` |
| 58 | Along-track local error (2 % wheel scale = ~12 cm rms per lap in the sandbox) is not observable from lane edges | 8 / on site | TODO | Only landmarks fix it (stop line, corners, roundabout). Check the real encoder scale in step 6 (IMU + odometry) and consider a landmark-based along-track update in block 05 (needs the team: V4 has none) |
| 59 | Step 4 stalls on the car (owner report 2026-09-24, page shows 'waiting for camera pictures' / 'solving camera mounts', still worded for several cameras) | 8 / on site | DOING | Wizard text fixed on the laptop (steps 2-4 say front camera only, step 4 says the solve takes 10-40 s on the RDK). The solver itself works on the real 640x480 frame offline (ground rms 10.2 mm PASS, mount x 0.229 y 0.015 z 0.080 yaw 1.3 pitch_down 3.3 roll 1.5, CAD delta 1.8 cm / 2.1 deg; it only failed because step 3 intrinsics were missing). New guard: `step_extrinsics_ipm.start()` refuses intrinsics captured at another image size than `cameras.yaml sensors.astra` (e.g. old 320x240) with a clear message. Still to check on the car: exact page message, frame tap under the saturated CPU (`capture_timeout_s: 20`), solve time on the RDK |  **Found on risabot5 10:30:** the wizard auto-resumed session `20260924_013152` (started 9 h earlier, `resume_max_age_h: 12`): step 3 showed PASS with intrinsics for **320x240** (rms 0.256 px) while the camera now runs 640x480, and the session's own `data/cameras.yaml` copy still had the old Astra settings. Guards added: step 3 `keep_data` refuses intrinsics for another size (StepRefused), step 4 `_solve` compares the intrinsics with the REAL picture size. Open hazard: race/calibrate launches read the ACTIVE session's `cameras.yaml` copy, so a stale copy would launch the Astra at its old width/height/launch file: after the first Save of a new session check `data/cameras.yaml` (astra launch_package/launch_file/launch_args, width/height/fps). Workaround used: `resume_max_age_h: 0.0` (always a new session) on the car for the recalibration
| 60 | Step 6: buttons to delete runs (`delete_run` per run, `delete_all_runs` per test; GUI Delete buttons with a confirm) | 8 | DONE (untested in a browser / on the car) | A run that was measured badly (car pushed crooked) no longer has to stay: the pass state is the LATEST run, so deleting it puts the previous one back in charge, and an unsaved step result computed from it is dropped (`invalidate_result` in `wizard_core._a_step`). A correction the run applied LIVE on servo_controller (ticks_per_meter, odom_reverse_polarity, imu_yaw_scale) is undone only when it is the newest run of its test and the live value is still the corrected one; otherwise it stays and the message says so (press Re-read). Refused while a measurement runs. `tabs_calib.js` node --check only |
| 61 | Step 6 distance could not pass on risabot5: the base servo_controller's `encoder_jump_threshold: 800` dropped nearly every encoder reading | 8 / on site | DONE in YAML (2026-09-24), verify run pending | Real wheel resolution ~286000 ticks/m (default assumed 1050): thousands of ticks per 50 ms, `Encoder jump filtered` ~20x/s (195 in one session), odom counted 0.09-0.9 m for 2.00 m pushes and the wizard's corrections chased it (462 -> ... -> 292197). With the threshold raised live to 100000 the runs converged (8.06 m, 6.15, 2.155, 1.959 m at tpm 21877 -> 88162 -> 271188 -> 292197). `base_nodes.yaml servo_controller.encoder_jump_threshold: 100000.0` now (was 800); the step-6 overlay saves ticks_per_meter, polarity, imu_yaw_scale but NOT this threshold. Check risabot1: its wheel may differ. Idea: step 6 should warn when a correction changes ticks_per_meter by more than 10x and when `Encoder jump filtered` is being logged |
| 62 | Step 6 distance calibrated by PULLING the car is wrong for motor driving; the launch does not re-apply the saved step 6 values | 8 / on site | DOING | risabot5 2026-09-24: hand-pulled runs converged on 293103 ticks/m (polarity on) and PASSED, but step 7 (motor driven, after a relaunch) counted 2.55 m for ~0.2 m of real travel. Two causes: (1) `calibrate.launch.py` loads NO session overlay (`calibration session: NONE`), so after a relaunch servo_controller runs base_nodes.yaml (ticks_per_meter 1050, polarity off) while step 6 saved something else; fixed: `ImuOdometryStep.background()` puts the saved values back once per session as soon as servo_controller answers (generic `StepImpl.background` hook in `wizard_core`, tested). (2) a wheel turned by hand does not give the encoder counts it gives when driven (noise / back-driven): step 6 distance text now says DRIVE it with the joystick (Manual control ON), never pull. The saved 293103 is WRONG: redo step 6 distance motor driven and Save. Open: measure real ticks/m on both cars with a tape; consider a motor-driven distance run inside step 6 |  **Follow-up 2026-09-24 13:20:** motor-driven runs stuck at ~0.2 m for 2.00 m: my live estimate 13400 ticks/m was ~10x too high, and `distance_min_fraction: 0.2` (0.4 m) rejected EVERY run as 'wheel not turning' with 'nothing changed', so the value never moved (15 runs). Now 0.02 (4 cm) and a test for exactly this case; live value set to 1340 by hand (odom 0.2 m x 13400/2.0). Note: setting a servo parameter behind the wizard's back leaves its cached 'servo_controller now' value stale: press Re-read values (or POST /api/calibration/action {step 6, STEP, {op: reread}}) before the next run.
| 63 | Step 7 stall boost + lite profile for the driving steps | 8 / on site | DONE (untested on the car) | risabot5 2026-09-24: steps 7/8 drove stop-and-go and barely moved at `raw_duty` 0.16. Causes: (a) under the full stack (load 23-26) the command owner ran at ~27 Hz instead of 50 and its 0.2 s request watchdog kept tripping (commanded steering dropped to 0 every few seconds); with `calibrate_profile:=lite` load fell to ~8 and the owner ran at 50 Hz; run `~/run_cal_lite.sh` for steps 7-9, use full for step 13. (b) 16 % duty may not overcome full-lock drag / friction: new `stall_*` keys in step 7 `procedure` (`stall_grace_s 1.5`, `stall_window_s 1.0`, `stall_min_progress_m 0.02`, `stall_boost_step 0.02`, `stall_boost_max_duty 0.30`): while the odometry moves < 2 cm per second the duty rises 0.02, capped (owner cap `calibration.duty_max` 0.35), back to `raw_duty` every segment; the page shows the duty and the boost count. Note: commanded speed in the Control plot is always 0 in CALIBRATION_RAW mode (`cmd_v` is 0 for raw), the duty is `out_linear_x`. Servo ranges raised live to 68/90 (centre 90): live only, a relaunch resets them (`servo_range_*` are not saved by step 7) |  **Kick (2026-09-24 evening):** a recording of a real run on risabot5 showed the whole chain (wizard -> owner -> servo_controller) passing duty 0.16 -> 0.30 (stall boost) and full-left steering exactly; the car rolled ~0.23 m at duty 0.20-0.24 and then stood still for 15 s at 0.30 (wheels spun, servo turned): static friction >> rolling friction. Step 7 now starts every segment with a kick (`kick_duty 0.40` for `kick_s 0.5`), falls back to `raw_duty`, repeats a stronger kick (`kick_step 0.05` up to `kick_max_duty 0.50`, `max_kicks 6`) on a stall and only then raises the cruise duty; `control.yaml command_owner.calibration.duty_max` 0.35 -> 0.50 so the kick is not clamped. Untested on the car. Still open: why the wheels spin without moving the car (tyre grip on this floor, new motor torque?)
| 64 | Dashboard (gui_server) on the owner's PC instead of the RDK | 8 / on site | PREPARED, not run (WSL not installed yet) | `tools/dashboard_pc/` (README, `setup_wsl.sh`, `run_gui_pc.sh`, `check_link.sh`, `wslconfig.example`), `carbot_bringup/launch/gui_pc.launch.py` (only gui_server, same params/domain as the robot), new launch arg `start_gui` (default true; `false` on the robot when the PC runs it). Needs WSL2 Ubuntu 22.04 + `networkingMode=mirrored` (Windows 11 build 26100 ok) so the robot's DDS multicast reaches ROS in WSL. Limits: tuning Save writes on the PC, not the robot's session; gui_server was ~60 % of a core on the RDK. Untested: DDS discovery over Wi-Fi from WSL, image previews |
| 65 | Race supervisor (real preflight/READY/START, 2026-09-25): `start_camera_uwb_agreement_m` and `auto_record` are declared but not used (no camera-map pose source; `run_recorder` not launched) | 8 | OPEN | Untested on the car. `start_pose_check_enabled` is FALSE (owner: assume the car is on P0); re-enable after UWB verify. The gate would stay red while UWB is unverified (step 10 verify skipped, lap ~1 m stretched, tag < 1 Hz) |

## How to add a request
Tell Claude in any chat, or add a row here yourself (next number, phase if known, status TODO).

## UWB: Haffiz's positioning (branch uwb-haffiz)

- Replaced the pairwise-trilateration + range-offset pipeline with Haffiz's method:
  closed-form linear solver + constant-velocity Kalman filter (`uwb_localization/positioning.py`,
  `common.yaml uwb_positioning`). See `docs/UWB.md`.
- Block 06 (`global_pose`) reads `localization.yaml global_pose.uwb_input` (RENAMED from
  `use_per_range_updates`); default `position` = his filtered `/carbot/uwb/position`.
- Step 10: offsets now optional (`offsets_mode`); Verify checks the filtered position.
- Step 11: new default mode `uwb_lap` (his position + `map_builder.py fit_rigid`, no odometry).
- NOT YET DONE: headless-browser check of the step 10 / step 11 / Localization GUI pages on the
  mock server (0 JS errors) — the code review and every pure-Python test pass, but the browser
  rendering itself was not re-verified after this change. Do this before trusting the wizard UI
  on the car.
- NOT tested on hardware: nothing in this branch has run against the real tag / anchors.
